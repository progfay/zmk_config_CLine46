#Bluetooth config -------------------
# 検証: eneloop使用時、TX Power+8dBmのバースト電流で電圧降下(ブラウンアウト)し
# sleep復帰後の再接続に失敗している疑いがあるため、左のみ既定値(0dBm)に戻す
# CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Zephyr 3.5 由来の BLE 接続安定性改善(split 再接続の信頼性向上)
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
